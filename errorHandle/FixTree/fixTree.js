export default async function fixTree(saleAccount,owner_id,invitation,usersSponsors,usersAccount,sponsors,user) {
    // first check the sirket hesabı errors 
    // then check sponsor errors
    // then check tree errors 
    // assume that users sponsors in user table is always correct

    // check sirket hesabı errors

    
}