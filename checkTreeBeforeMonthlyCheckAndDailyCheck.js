export default async function checkTreeBeforeMonthlyCheckAndDailyCheck() {
    // make sure first sponsors is correct 

    // then make sure tree is correct

    // then make sure the points in the tree is correct 

    // then make sure saleAccount realPoint1 is correct 

    // then look at the saleAccount and balance is correct 

    // then make sure the user is actve 

    // then make sure all refunds are in the persepective 

    // then check 1 months afters tree and all things then make sure 

    // then repeat 4 times

    
}