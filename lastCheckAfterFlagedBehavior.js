export default async function lastCheckAfterFlagedBehavior(thisSaleAccount,user,sponsors,invitation,SaleAccount){
    // now we know sponsors are right 
    // tree is right 
    // points are right
    //flagedBehavior is done 
    // now we can do the last check 

    
}